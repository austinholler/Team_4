1. SSH to AWS
2. From Team_4 directory, "git pull" (may need to git stash first)
3. Modify secret.js to have correct secret keys. (if it doesn't already)
5. Edit cacheHandler.js and switch "prod" boolean to true (if it isn't)
6. Edit scripts/updateCache.py and switch localhost to the URL of the redis cluster (if it isn't already)
7. Sudo forever stopall
8. Sudo forever start app.js

